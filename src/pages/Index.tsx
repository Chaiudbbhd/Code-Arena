import { AuthCard } from "@/components/auth/AuthCard";

const Index = () => {
  // For the demo, we show the auth screen
  // In a real app, this would redirect to dashboard if authenticated
  return <AuthCard />;
};

export default Index;
